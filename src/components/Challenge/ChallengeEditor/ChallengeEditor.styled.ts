import { Box, styled } from "decentraland-ui2"

const ChallengeEditorContainer = styled(Box)({
  display: "flex",
  flexDirection: "column",
  gap: "10px",
  marginBottom: "20px",
})

export { ChallengeEditorContainer }
